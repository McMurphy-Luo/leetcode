#include <string>

using std::string;

class Solution {
public:
  string convertToTitle(int n) {
    int remainder = n;
    string result;

    while (remainder > 0) {

    }

    
  }
};